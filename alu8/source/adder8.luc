module adder8 (
    input a[8],
    input b[8],
    input alufn[6],
    output out[8]
  ) {
  
  always {
    if(alufn[0]) {
      out = a - b;
    }
    else {
      out = a + b;
    }
  }
}